MLLM 5.2 preview — Lightweight Autocomplete Micro Language Model
================================================================
MLLM-5.2 preview corpus file — lightweight autocomplete models
----------------------------------------------------------------
PURPOSE:
  This is your placeholder/template corpus for MLLM-5.2.
  Lightweight autocomplete — tiny, local, BYO corpus. Your corpus is
  the personality; ghost-text learns your style.
  You will make a corpus yourself: replace everything below the
  sentinel line with your own text. Lightweight and local by design.

HOW TO USE — 2 steps:
  1) Edit: delete EXAMPLE block below, paste your text under PASTE line.
     Keep filename exactly "MLLM 5.2 preview" (spaces, no extension).
  2) Run:  python MLLM-5.2.py --corpus "MLLM 5.2 preview" "your prefix" --steps 16
  Ghost: type prefix → dim ghost-text → Tab to accept, Esc to dismiss.

FORMAT GUIDANCE:
  - Plain UTF-8 text, one sentence per line ideal.
  - Keep punctuation (. ! ?) — meaningful for generation.
  - Blank lines OK (treated as separators).
  - Size sweet spot: 5KB–500KB. Smaller fine for testing.
  - Tokenized lowercased as \b[a-zA-Z0-9']+\b|[.!?]

PRIVACY:
  Your corpus stays local, never uploaded. Training and generation
  happen fully on-device — nothing leaves your machine.

COMING SOON:
  ✨ MLLM Document Editor — coming soon. Will hot-load this file
  for live ghost-text editing — type → ghost → Tab — no restart.

TAGLINE:
  MLLM-5.2 — Lightweight autocomplete models, trained on your words.

----------------------------------------------------------------
EXAMPLE — DELETE AND REPLACE
  ⚠️  Delete this entire EXAMPLE block before adding your corpus.
  It WILL be tokenized if you leave it — remove it first.
----------------------------------------------------------------
The quick brown fox jumps over the lazy dog.
Hello world, this is a preview corpus.
Lightweight autocomplete models adapt to your style.

----------------------------------------------------------------
### PASTE YOUR CORPUS BELOW THIS LINE ###
----------------------------------------------------------------

### YOUR CORPUS STARTS HERE — replace everything below ###

[ paste your corpus here — delete this line and start typing.
  Tip: one sentence per line gives the smoothest ghost results. ]

